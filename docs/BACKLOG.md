# Backlog

## 📌 PINNED · infolog.io Bento — data-viz integration (BLOCKED)

**Pinned:** 2026-05-30 · **Status:** waiting on dependency (data-viz skill)

The infolog.io homepage redesign is built and chosen: the **Bento** macrostructure
(Linen theme) at `~/Developer/infolog.io/bento.html`. Content clipping is fixed,
responsive 320–1280 px (verified), contrast-checked.

**Blocked on:** the infolog.io **data-visualization skill** — in progress on a
parallel thread, not yet on this machine as of 2026-05-30. House style is
Tufte / minimal (reference outputs: `~/Developer/piper-marketing/working-files/piper-data-visualization/*.svg`).

**Where it goes:** the **track-record stat tile** in `bento.html`. Turn
`100+ teams · 150+ JS products · 3 US patents · 20 years` into one honest,
Tufte-style visual.

**Unblock when the skill is done →**
1. Read the data-viz skill.
2. Build the stat-tile visual; replace the plain `.tile--stat` strip.
3. Re-verify responsive (320/375/414/768) + contrast, then ship.

**Also pending in the same file:** booking URL, LinkedIn/GitHub URLs, six essay
URLs (all marked `REPLACE`). Headshot intentionally omitted in this layout.

_Sibling variants built (same copy, different structure/theme), for reference:_
`index.html` (Newsprint), `green.html`, `midnight.html`, `brutal.html`,
`manifesto.html`, `stat-led.html` — all in `~/Developer/infolog.io/`.
